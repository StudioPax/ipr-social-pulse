// @page Settings — Client configuration, social accounts, API tokens
// App Spec §1.2 — Client Manager UI

export default function SettingsPage() {
  return (
    <div className="p-6">
      <h2 className="font-display text-2xl">Settings</h2>
      <p className="mt-2 text-sm text-muted-foreground">
        Manage clients, social accounts, and API connections.
      </p>
      {/* TODO: Implement settings UI (Phase 4) */}
    </div>
  );
}
